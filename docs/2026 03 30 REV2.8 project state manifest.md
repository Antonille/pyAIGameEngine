# 2026 03 30 REV2.8 project state manifest

## Workspace version
0.0.19

## POC package version
0.1.11

## Current truth summary
- POC1 fallback path remains authoritative
- PyBullet remains optional/deferred
- research sweep remains a documented procedure for checking architecture direction before widening implementation
- packet boundary semantics include versioned generations, baseline references, supersede references, explicit cache outcomes, and support for replace / delta / invalidate / no_change / delete modes
- explicit `no_change` behavior is exercised by a dedicated hold/steady family and is visible in demo and benchmark output
- packet ownership semantics now distinguish ownership scope from concrete ownership key
- the latest validated benchmark subset recorded in the docs still shows:
  - `500 / 1024`: warmed **Numba** wins
  - `2000 / 1024`: warmed **Numba** wins
- REV2.8 intentionally preserved that benchmark truth pending a fresh benchmark-compare pass for the ownership-aware workload

## Current phase
- documentation/control-boundary formalization complete for model/AI/UI roles
- multi-family runtime-schema / feature-pack / transfer-planner boundary implemented and validated
- versioned action-packet bridge, packet cache, and packet-state modes implemented and validated
- explicit `no_change` path implemented and benchmark-compared
- packet ownership / baseline semantics refined
- next major work block: benchmark-compare the ownership-aware no-change-enabled workload or prepare project relay

## Highest-priority next step
Run a benchmark-compare pass for the ownership-aware no-change-enabled packet workload and decide whether the packet boundary is ready for project relay.
