# 2026 03 28 REV1.3 Progress Notes

## Checkpoint summary
- Consolidated the project around `C:\PythonDev\Dev1\pyGames\pyAIGameEngine`.
- Removed the obsolete `pyGameEngine` / Python 3.14.x assumptions from active-path documents.
- Reorganized docs into `docs/current/` and `docs/archive/`.
- Promoted the current-truth docs to REV1.3 and archived earlier mixed REV1.0/REV1.1/REV1.2 material.
- Updated snapshot discipline so full milestone snapshots are explicitly clean.
- Updated `install_project.ps1` so it creates `.venv` if missing.
- Repaired `rebuild_venv_py312.ps1` to match the new bootstrap behavior.
- Added 7-Zip-based clean snapshot scripts and an exclude-pattern file.
- Cleaned the delivered full snapshot by removing transient artifacts.
- Repaired the corrupted Windows command examples in `POC1_SoaFirst/README.md`.

## Current validated project position
- workspace is treated as **0.0.8**
- active proof of concept is **POC1_SoaFirst**
- authoritative runtime path is **fallback-first CPU**
- NumPy / Numba simple backend validation path is active
- PyBullet remains optional / deferred

## What remains next
- run the updated install/bootstrap flow locally and confirm `.venv` creation works from scratch
- keep benchmark comparison tooling improving
- begin the first reusable AI scheduling/runtime-schema implementation pass
