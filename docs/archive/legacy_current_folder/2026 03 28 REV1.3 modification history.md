# 2026 03 28 REV1.3 Modification History

## REV1.3 / workspace 0.0.8
- moved legacy docs into `docs/archive/2026-03-28_pre_rev1.3/`
- created a focused `docs/current/` set plus current aliases
- updated the active project path and baseline assumptions to `pyAIGameEngine` + Python 3.12
- updated install/bootstrap behavior so the install script creates `.venv` if missing
- repaired the rebuild-venv script
- added 7-Zip-based clean snapshot scripts and exclude patterns
- cleaned the delivered full snapshot by removing transient artifacts
- repaired corrupted command examples in `POC1_SoaFirst/README.md`
