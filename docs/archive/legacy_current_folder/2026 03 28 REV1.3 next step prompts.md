# 2026 03 28 REV1.3 Next Step Prompts

## Immediate prompt: bootstrap verification + benchmark discipline
Use the current pyAIGameEngine project state as truth.

Task:
Verify the refreshed documentation and bootstrap/install path against the current codebase.

Requirements:
1. Assume `install_project.ps1` now creates `.venv` if missing.
2. Verify that the documented first-run commands are still correct for the single Python 3.12 baseline.
3. Tighten benchmark comparison output between NumPy and Numba modes.
4. Update any affected current docs and append file modification history.
5. Deliver:
   - sparse snapshot ZIP
   - updated docs/scripts if needed
   - exact local validation commands
   - next recommended prompt

## Immediate prompt: first reusable AI seam
Use the current pyAIGameEngine project state as truth.

Task:
Proceed with the first implementation-oriented pass for reusable AI scheduling/runtime-schema infrastructure.

Requirements:
1. Preserve fallback-first execution and keep POC1 authoritative for active runtime validation.
2. Build small CPU-side scheduling structures for one repeated object family.
3. Define fixed feature blocks and a precomputed feature-pack plan for that family.
4. Keep CPU state authoritative and stage-oriented.
5. Update current docs, prompts, and file modification history.
6. Deliver:
   - sparse snapshot ZIP
   - updated code and docs
   - exact local validation commands
   - next recommended prompt
