# Prompt Templates — REV1.1
Date: 2026-03-26
Revision: 1.1

## Next recommended implementation prompt
Use the current pyAIGameEngine project state as truth.

Task:
Continue POC1_SoaFirst toward the minimally functional milestone using the fallback backend as the default runnable path.

Requirements:
1. Add explicit engine plank-tic stage modules and timing hooks.
2. Keep PyBullet as an optional backend and do not make it the default validation path.
3. Preserve renderer abstraction and keep viewer optional.
4. Update docs, revision tracking, and workflow notes.
5. Deliver a sparse snapshot ZIP of changed files only.

## Native toolchain prompt
Use the current pyAIGameEngine project state as truth.

Task:
Help validate whether the Visual Studio Developer shell is correctly exposing MSVC and the Windows SDK for PyBullet source builds.

Requirements:
1. Review the output of check_msvc_env.ps1.
2. Determine whether PyBullet install should succeed in the current shell.
3. Recommend the exact next shell/command sequence.
