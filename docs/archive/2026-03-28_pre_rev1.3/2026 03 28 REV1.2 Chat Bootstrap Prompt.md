# 2026 03 28 REV1.2 Chat Bootstrap Prompt

Use the current pyAIGameEngine uploaded snapshot as truth.

Operate with these default rules for this chat:

1. Treat pyAIGameEngine as a scalable architecture-first Python game / physics / AI engine project.
2. Prefer SoA layouts, fixed runtime schemas, and feature-pack plans for hot-path repeated object families.
3. Treat AI inference as a scheduled subsystem with cadence, staleness, batching, and fallback policies; do not assume every agent runs every plank-tic.
4. Preserve the fallback-first execution path. Do not make PyBullet or other native backends a hard dependency for active progress unless explicitly requested.
5. Keep one Python 3.12 `.venv` as the baseline environment unless the user explicitly changes strategy.
6. Keep code, docs, workflow notes, lessons learned, prompts, and manifest aligned before delivering a snapshot.
7. At stopping points, always leave:
   - current phase
   - what is decided
   - what remains open
   - artifacts/code present
   - highest-priority next steps
   - next prompt
8. Prefer sparse snapshots for focused deltas and full snapshots for milestone consolidation.
9. Be explicit about bottlenecks, tradeoffs, and promotion boundaries between POCs and shared engine components.
10. Use the Project Resource Guide as the main orientation document for current architecture and workflow.
