# 2026 03 28 REV1.2 progress notes

## Summary
The project snapshot has been consolidated and refreshed against the locally uploaded package. Documentation is now aligned to the implemented fallback-first POC1 path, the NumPy/Numba backend mode split, and the newly adopted AI scheduling / runtime-schema direction.

## Confirmed code status
- POC1 benchmark, rollout, viewer, and Numba experiment have been exercised successfully on the fallback path
- event-processing overhead was identified as a major cost center and partially reduced
- validation script was hardened to fail on benchmark/rollout failure
- backend selection logic was aligned to fallback-first behavior

## Architecture status
- AI scheduling is still primarily architectural truth, not yet full implementation
- runtime schema + feature-pack approach is now accepted design guidance for repeated object families and AI feature gathering
