# Next-Step Prompt Suggestions — REV1.0

## Suggested immediate next prompt
```text
Use the Python-3.12-baselined pyAIGameEngine snapshot as the new truth.
Goals:
1. Confirm the single `.venv` is running on Python 3.12 and that the baseline package set imports cleanly.
2. Add an explicit engine loop module naming the per-plank-tic stages.
3. Add a Numba micro-benchmark comparing NumPy vs Numba integration on large SoA arrays.
4. Update all REV1.0 tracking docs affected by the new work.
5. Deliver a sparse snapshot ZIP and a concise validation summary.
Before delivering, perform best-practice actions captured in the snapshot docs, including updating prompts, modification history, and next-step suggestions.
```

## Suggested milestone prompt after that
```text
Continue toward minimally functional POC1 on the Python 3.12 single-venv baseline.
Implement the smallest stable end-to-end demo that exercises:
- SoA spawn/setup
- PyBullet stepping
- viewer rendering
- single-agent Gym-compatible reset/step loop
- timing harness output
Update workflow/progress/lessons/modification docs and deliver a sparse snapshot ZIP.
```
