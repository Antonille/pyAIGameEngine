# 2026 03 30 REV3.1 Current Workflow

## Current phase
Early architecture definition + validated fallback POC1 + packet-boundary and ownership-aware benchmark formalization complete + lifecycle / V&V / simulation-credibility / AI-RL governance scaffolding added.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to **current validated truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide **exact validation commands** where validation is relevant.
6. Update current-truth docs, lessons, prompts, manifest/history, and handoff artifacts.
7. Ask whether to use `project relay` or `chat retrospective` when a meaningful block has just completed.

## Governance guidance now in force
Before widening implementation significantly again, use the new governance documents to check:
- lifecycle phase and phase gate target
- current V&V coverage
- current simulation credibility scope
- AI/RL readiness and intended integration order
- artifact/versioning expectations for new experiments or candidates

## Research sweep guidance
Use `research sweep` when:
- the benchmark signal is mixed
- the architecture branch is about to widen
- a new tool/backend path is being considered
- the current design needs an outside-evidence check before more implementation

## Benchmark compare guidance
Use `benchmark compare` when:
- the integrated winner claim may have changed
- a new family, packet contract, cache policy, or ownership rule has changed the workload mix
- stage-level and packet-level metrics need to be reconciled into current project truth
- the next implementation step depends on knowing whether the winner claim is still valid

## Documentation/interface guidance
Use `documentation/interface pass` when:
- lifecycle/governance docs need to catch up with implementation reality
- current-pointer navigation should be widened
- interface-control documents are ahead of or behind code reality
- the next implementation block would benefit from clearer phase gates or verification targets

## Relay trigger guidance
The assistant should ask about `project relay` after a meaningful architecture/doc/implementation block, especially after:
- validated outputs and doc refreshes
- several sparse refreshes
- interface/control document changes
- milestone completion or near-completion

This should be a soft question, not a forced stop.

## Current benchmark-compare takeaway
For the current ownership-aware `no_change` / baseline-ownership-targeted packet workload subset:
- the smoke run confirms instrumentation and packet-state visibility
- medium and long representative runs both favor warmed **Numba**
- packet-layer metrics match across backends for the same run
- the integrated winner claim is therefore currently stable for this validated workload subset

## Near-term sequence now recommended
1. Optionally perform `project relay` after this lifecycle/V&V/governance docs refresh block
2. Use the new Lifecycle and Phase Gates + V&V Matrix to decide the next implementation gate consciously
3. Reopen implementation only when the next work block names which phase gate it is trying to clear
