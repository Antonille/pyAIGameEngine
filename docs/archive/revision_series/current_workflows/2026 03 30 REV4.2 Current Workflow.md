# 2026 03 30 REV4.2 Current Workflow

## Current phase
GitHub-first workflow is active. The first working slice of the long-lifecycle integrated testing and reporting system is now accepted, and uploaded archival test outputs have been integrated as current accepted testing truth.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to the best available **current truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide exact validation commands where validation is relevant.
6. Update current docs, report outputs, lessons, prompts, and handoff artifacts.
7. Commit and push the intended GitHub state.
8. Export sparse/full snapshots only when useful for archive, relay, rollback, or transfer.

## Current truth precedence
1. pushed GitHub repo state on the intended branch
2. newer uploaded archival test outputs / local unpushed evidence
3. uploaded candidate snapshots
4. older archived snapshots

## Accepted testing/reporting workflow now in force
- archival test data is append-only durable truth
- the current human-readable report is regenerable from archival data plus referenced artifacts
- uploaded generated artifacts may be accepted as current report inputs when they are newer than GitHub
- after analysis/report-regeneration passes, accepted outputs should be reconciled back into GitHub promptly

## Near-term priorities
1. reconcile accepted report/docs updates back into GitHub
2. tighten AI/RL adapter/interface docs so the current adapter path does not quietly become the wrong long-term abstraction
3. keep benchmark comparison tooling and accepted benchmark history current
4. defer larger refactors until the adapter/action-packet boundary is clarified with the new accepted test truth
