# 2026 03 30 REV2.3 Current Workflow

## Current phase
Early architecture definition + validated fallback POC1 + interface-definition pass complete + retrospective-hardened documentation workflow + dual-family runtime-schema / feature-pack / transfer-planning boundary with action-packet bridge implemented + research-sweep validation of the current direction.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to **current validated truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide **exact validation commands** where validation is relevant.
6. Update current-truth docs, lessons, prompts, and handoff artifacts.
7. Ask whether to use `project relay` or `chat retrospective` when a meaningful block has just completed.

## Research sweep guidance
Use `research sweep` when:
- the benchmark signal is mixed
- the architecture branch is about to widen
- a new tool/backend path is being considered
- the current design needs an outside-evidence check before more implementation

A research sweep should:
1. review current-pointer docs first
2. search for similar architectures, batching strategies, runtime layouts, and tooling paths
3. separate findings into recommended-now / plausible-later / deferred
4. update lessons, workflow, prompts, and resource docs
5. narrow the next step into either an architecture/spec pass or an implementation pass

## Relay trigger guidance
The assistant should ask about `project relay` after a meaningful architecture/doc/implementation block, especially after:
- validated outputs and doc refreshes
- several sparse refreshes
- interface/control document changes
- milestone completion or near-completion

This should be a soft question, not a forced stop.

## Chat retrospective guidance
Use `chat retrospective` when a just-completed block generated reusable lessons about:
- prompt structure
- workflow efficiency
- validation discipline
- benchmark interpretation
- relay timing
- interface/control-boundary timing

A retrospective may be performed:
- as its own docs-first pass
- before a project relay
- after a relay reconciliation if new workflow lessons emerged during the relay

## Expected outputs from `research sweep`
1. updated lessons learned
2. updated workflow/resource docs
3. updated optimized prompt templates
4. tool / library candidates with gates
5. recommended-now vs deferred summary
6. concise architecture-alignment judgment
7. recommended next prompt

## Near-term sequence after the research sweep
1. Architecture/spec pass for action-packet cache semantics, baseline/generation semantics, and benchmark-matrix definition
2. Implementation pass for packet caching plus another action contract variant
3. Keep benchmark comparison tooling current and compare integrated winner claims again whenever family count, cadence mix, packet width, or caching behavior changes
4. Delay larger refactors until the packet boundary is proven across richer semantics
5. Consider `project relay` after the next meaningful validated implementation block
