# 2026 03 30 REV3.9 Current Workflow

## Current phase
Post-Gate-D and research-aligned documentation refinement. Phase 4 Gate A remains the next implementation gate, but the next implementation branch should carry forward better testing/reporting and benchmark-history discipline.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to **current validated truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide **exact validation commands** where validation is relevant.
6. Update current-truth docs, lessons, prompts, manifest/history, and handoff artifacts.
7. Ask whether to use `project relay` or `chat retrospective` when a meaningful block has just completed.

## Research sweep guidance now in force
A research sweep should:
1. review current-pointer docs first
2. use attached research inputs as prior context, not as the only source
3. generate at least **two complementary research topics** not already covered by the attached input
4. separate findings into:
   - recommended now
   - plausible later
   - deferred / not yet
5. fold the results into docs, lessons, prompts, and next-step guidance

## Benchmark and testing workflow guidance
- Continue using current lightweight benchmark scripts now.
- When extending the testing/reporting system, record:
  - fixed seeds
  - compatibility keys for apples-to-apples comparison
  - machine/runtime metadata where practical
  - explicit test mode tags
- Treat archival test data as append-only truth and current reports as regenerable products.
- Keep future adoption of stronger history tooling possible by designing outputs to be easy to compare over time.

## Adapter-facing workflow guidance
For current adapter-facing work:
- keep the adapter boundary thin and non-authoritative
- preserve contract-facing observations/actions
- follow explicit seeding discipline
- keep `train`, `eval`, and `benchmark` modes explicit and recorded

## Near-term sequence now recommended
1. Clear Phase 4 Gate A
2. Implement the first working slice of the long-lifecycle testing/reporting system
3. Use that archival/reporting slice to support future benchmark-history and invariant-testing expansion
4. Revisit broader RL-facing work only after the reporting/testing loop exists and adapter-facing metadata discipline is proven
