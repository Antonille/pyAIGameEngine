# 2026 03 30 REV4.3 Current Workflow

## Current phase
GitHub-first workflow active on top of the fallback-first POC baseline, with accepted archival testing truth in place and AI/RL boundary hardening underway.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to the best available **current truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide exact validation commands where relevant.
6. Update current docs, lessons, prompts, and manifests.
7. Commit and push the intended GitHub state.
8. Export sparse/full snapshots only when useful for archive, relay, rollback, or recovery.

## Near-term implementation order
1. deterministic reset / replay plumbing
2. replay-friendly state capture hooks
3. adapter-facing timing hook surfacing
4. minimal env adapter refit toward packet-oriented action flow
5. snapshot metadata generation fix if still outstanding

## Current caution points
- Do not freeze a public RL API around the current Gym path.
- Do not leak model-specific logic into the hot simulation core.
- Keep many-world / headless parallel rollout future-compatible but deferred.
