# 2026 03 30 REV3.6 Current Workflow

## Current phase
Post-Gate-D architecture/spec state. Phase 4 Gate A is defined as the next implementation gate.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to **current validated truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide **exact validation commands** where validation is relevant.
6. Update current-truth docs, lessons, prompts, manifest/history, and handoff artifacts.
7. Ask whether to use `project relay` or `chat retrospective` when a meaningful block has just completed.

## Gate guidance now in force
Before reopening code, name the gate being targeted and the evidence that will clear it.

### Current next gate
**Phase 4 Gate A — Environment Adapter and Baseline Policy Readiness**

### Gate A implementation intent
The next implementation block should:
- add a minimal, non-authoritative adapter prototype path
- add two baseline policies:
  - hold / steady
  - simple reflex
- add explicit mode tagging for `train`, `eval`, and `benchmark`
- add artifact fields for scenario/adaptor/policy/seed tracking
- prove those behaviors with explicit Gate A acceptance evidence

## Near-term sequence now recommended
1. Clear Phase 4 Gate A
2. Use the cleared adapter/baseline boundary to define the first scenario bundle and evaluation harness gate
3. Revisit broader RL-facing work only after those gates are explicit and evidenced
