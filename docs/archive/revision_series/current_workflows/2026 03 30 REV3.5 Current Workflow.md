# 2026 03 30 REV3.5 Current Workflow

## Current phase
Phase 3 active with Gate D cleared for current prototype scope; current benchmark truth preserved; next gate should define environment-adapter / baseline-policy readiness before broader AI/RL-facing code work resumes.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to **current validated truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide **exact validation commands** where validation is relevant.
6. Update current-truth docs, lessons, prompts, manifest/history, and handoff artifacts.
7. Ask whether to use `project relay` or `chat retrospective` when a meaningful block has just completed.

## Governance guidance now in force
Before widening implementation significantly again, use the governance documents to check:
- lifecycle phase and phase gate target
- current V&V coverage
- current simulation credibility scope
- AI/RL readiness and intended integration order
- artifact/versioning expectations for new experiments or candidates

## Gate-clearing guidance now in force
When an implementation pass is explicitly clearing a phase gate, it should:
- name the gate at the top of the pass
- add or strengthen explicit acceptance checks
- say what evidence counts toward clearing the gate
- end with a gate conclusion: cleared / partially cleared / blocked
- update lifecycle and V&V docs to reflect the result

## Current benchmark-compare takeaway
For the current ownership-aware `no_change` / baseline-ownership-targeted packet workload subset:
- the smoke run confirms instrumentation and packet-state visibility
- medium and long representative runs currently favor warmed **Numba** in the last validated benchmark-compare pass
- packet-layer metrics match across backends for the same compared run
- the integrated winner claim remains stable for the last validated recorded subset until explicitly superseded by a new benchmark-compare pass

## Near-term sequence now recommended
1. Define the next gate: **Phase 4 Gate A — Environment Adapter and Baseline Policy Readiness**
2. Use an **architecture/spec pass** to define the adapter boundary, baseline-policy scope, train/eval separation, and acceptance criteria for that gate
3. Reopen implementation only after that gate is named and scoped
4. Use `project relay` again after the next meaningful architecture/spec or implementation milestone
