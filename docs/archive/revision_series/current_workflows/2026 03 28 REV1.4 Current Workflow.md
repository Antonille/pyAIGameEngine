# 2026 03 28 REV1.4 Current Workflow

## Current phase
Early architecture definition + POC1 fallback-path implementation + AI scheduling/runtime-schema planning.

## Current validated path
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\windowsun_poc1_validation.ps1 -BackendMode numpy
powershell -ExecutionPolicy Bypass -File .\scripts\windowsun_poc1_validation.ps1 -BackendMode numba
```

## Current interpretation of benchmark results
- fallback validation passes in both numpy and numba modes
- with warmup and comparison tooling, numba can slightly outperform numpy in the integrated benchmark
- event processing is no longer the dominant bottleneck
- render snapshot and physics remain important stage costs
- AI scheduling prototype already surfaces scheduled model families in the benchmark output

## Current design focus
1. runtime schemas for repeated families
2. feature-block / feature-pack planning
3. transfer planning between scheduled AI families and staged copy groups
4. field/runtime-schema prototype work
5. preserve fallback-first execution and PyBullet optional/deferred status
