# 2026 03 30 REV3.3 Current Workflow

## Current phase
Relay-ready candidate after validated ownership-aware packet benchmarking and lifecycle / V&V / simulation-credibility / AI-RL governance expansion, with current-pointer and filename consistency reconciled.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to **current validated truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide **exact validation commands** where validation is relevant.
6. Update current-truth docs, lessons, prompts, manifest/history, and handoff artifacts.
7. Ask whether to use `project relay` or `chat retrospective` when a meaningful block has just completed.

## Governance guidance now in force
Before widening implementation significantly again, use the governance documents to check:
- lifecycle phase and phase gate target
- current V&V coverage
- current simulation credibility scope
- AI/RL readiness and intended integration order
- artifact/versioning expectations for new experiments or candidates

## Relay guidance
Use `project relay` when:
- validated outputs and doc refreshes have accumulated
- several sparse refreshes have occurred since the last full snapshot
- governance docs or interface-control docs have changed substantially
- a clean candidate package is needed for a new chat or handoff

A successful relay should:
- reconcile the latest candidate state against accumulated sparse passes
- refresh current-pointer docs
- refresh manifest and modification history
- refresh bootstrap and relay prompts
- package a clean relay-ready candidate snapshot
- confirm that current-pointer docs resolve to actual clean filenames in the candidate package

## Current benchmark-compare takeaway
For the current ownership-aware `no_change` / baseline-ownership-targeted packet workload subset:
- the smoke run confirms instrumentation and packet-state visibility
- medium and long representative runs both favor warmed **Numba**
- packet-layer metrics match across backends for the same run
- the integrated winner claim is therefore currently stable for this validated workload subset

## Near-term sequence now recommended
1. Use the Lifecycle and Phase Gates + Verification and Validation Matrix to choose the next implementation gate consciously
2. Reopen implementation only when the next work block names which phase gate it is trying to clear and what evidence will validate it
3. Use `project relay` again after the next meaningful architecture/spec or implementation milestone
