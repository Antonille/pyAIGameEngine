# 2026 03 30 REV4.4 Current Next-Step Prompts

## Primary next prompt
Use the current pyAIGameEngine public GitHub main branch as repo-truth.

Pass type:
Implementation pass.

Task:
Perform a bounded repo-maintenance and snapshot-workflow hardening pass.
Do not do new engine/AI feature work in this pass.

Truth to preserve:
- fallback-first CPU-authoritative path remains current truth
- PyBullet stays optional/deferred unless validated work changes that
- public GitHub main is authoritative repo-truth for this pass
- REV4.4 current-pointer docs and the lesson-action register are now part of the active workflow surface

Pre-pass retrieval required:
1. Read `docs/CURRENT_LESSONS_LEARNED.md`
2. Read `docs/CURRENT_LESSON_ACTION_REGISTER.md`
3. State which open actions from the register are being addressed in this pass

Required work:
A. Replace the brittle latest-snapshot selection behavior with two explicit compatible apply scripts:
   - one for `pwsh`
   - one for Windows PowerShell 5.1 (`powershell`)
B. Make snapshot selection explicit and deterministic:
   - support `Kind` selection such as `full|sparse|patch|any`
   - prevent utility ZIPs such as `snapshot_script_updates_*` from being treated as project snapshots
   - report what was selected and why
C. Clean up the report/artifact path policy:
   - make the durable archival path vs regenerable current-report path explicit
   - add placeholders or README notes if needed
D. Refresh stale repo entry points that currently lag behind the current-pointer doc family:
   - root `README.md`
   - any workflow-facing surfaces that materially conflict with current truth
E. Update docs/workflow/history as needed and leave exact local validation commands plus a relay-quality handoff

Deliverables:
1. updated scripts and docs
2. exact local validation commands
3. concise summary of what changed
4. relay-quality handoff
5. next recommended prompt
