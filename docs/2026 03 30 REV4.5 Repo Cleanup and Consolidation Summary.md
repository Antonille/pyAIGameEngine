# 2026 03 30 REV4.5 Repo Cleanup and Consolidation Summary

## Purpose
Record the bounded cleanup/pruning/archiving/concatenation pass that turned the assembled package into a cleaner full snapshot.

## What this pass did
- pruned transient/editor artifacts (`__pycache__`, `.pyc`, `.vscode`, sparse-refresh helper files)
- removed legacy root text files that were snapshot-transfer aids rather than durable repo-truth
- moved stale `docs/current/` contents out of the active surface
- moved non-active revision docs out of the docs root and into archive series
- concatenated archived modification-history and project-state-manifest series into two consolidated archive files
- refreshed active workflow docs to REV4.5 where cleanup/process truth changed
- updated root README, POC1 README, artifact/report readmes, and Windows script README to current standards
- added an explicit full-snapshot replacement helper script

## Cleanup principles applied
- keep the active surface small and restart-friendly
- preserve history, but archive it where it stops competing with current truth
- prefer current pointers and curated current docs over duplicate folder surfaces
- remove transient files from both repo and future snapshots
- keep lessons actionable and evidence-backed

## Known open risks after this cleanup
- legacy latest-snapshot selector logic still needs a dedicated implementation pass
- public GitHub main may still lag behind this cleaned snapshot until it is applied and committed
- some current-target docs still intentionally point to older validated revisions when no newer replacement has been accepted yet

## Expected outcome
This snapshot should make the repo easier to read, easier to relay, and less likely to mislead the next pass about what is current versus historical.
