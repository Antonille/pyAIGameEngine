# 2026 03 30 REV3.0 project state manifest

## Workspace version
0.0.20

## POC package version
0.1.12

## Current truth summary
- POC1 fallback path remains authoritative
- PyBullet remains optional/deferred
- research sweep remains a documented procedure for checking architecture direction before widening implementation
- packet boundary semantics include versioned generations, cache outcomes, explicit `no_change`, delete semantics, baseline/supersede references, ownership scope/key semantics, and baseline/superseded ownership targeting
- explicit `no_change` behavior is exercised by a dedicated hold/steady family and is visible in demo and benchmark output
- packet ownership semantics distinguish ownership scope from concrete ownership key
- ownership-aware benchmark subset now recorded in the docs shows:
  - `500 / 1024`: warmed **Numba** wins
  - `2000 / 1024`: warmed **Numba** wins
- packet-layer metrics match across backends for the same compared run in the ownership-aware subset
- ownership-targeted semantics reduced explicit invalidate traffic and packet churn in the validated workload subset

## Current phase
- documentation/control-boundary formalization complete for model/AI/UI roles
- multi-family runtime-schema / feature-pack / transfer-planner boundary implemented and validated
- versioned action-packet bridge, packet cache, and packet-state modes implemented and validated
- explicit `no_change` path implemented and benchmark-compared
- packet ownership / baseline semantics refined again with ownership-target keys
- ownership-aware benchmark subset recorded and interpreted
- next major work block: documentation/interface pass for lifecycle management, V&V, simulation credibility, and AI/RL integration strategy, or project relay

## Highest-priority next step
Perform a documentation/interface pass to incorporate lifecycle-management, verification, simulation-credibility, and AI/RL-integration best practices into the working project docs.
