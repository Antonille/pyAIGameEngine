# 2026 03 29 REV1.8 pyAIGameEngine Project Instructions

## Purpose
These are the full working instructions for the pyAIGameEngine project. They complement the concise project-instructions text used in ChatGPT project settings and serve as the durable long-form reference for workflow, tracking, restartability, and project operating style.

## Current environment truth
- Local project path: `C:\PythonDev\Dev1\pyGames\pyAIGameEngine`
- Python baseline: **single local Python 3.12 `.venv`**
- Current workspace version target in this snapshot: **0.0.13**
- Active proof of concept: **POC1_SoaFirst**
- Active validated execution path: **fallback-first CPU path**
- Native physics status: **PyBullet optional / deferred**
- GitHub username: `Antonille`

## Core project concepts and vocabulary
Use the following terms consistently unless later revised:
- **plank-tic**: smallest unit of simulation/game time. All in-simulation time differences are integer numbers of plank-tics.
- **plank-pix**: smallest distance unit.
- **voxellino**: smallest scalar-valued volume element.
- **voxel**: smallest cubic boundary/surface element used to describe geometry.
- **coordinate system**: a packed set of voxels.
- **master stationary reference frame**: the coordinate system containing the extent of all others.
- **shapes/objects**: entities defined relative to coordinate systems, with surface/boundary and property descriptions.

## Project direction
The engine architecture should prioritize:
- scalable architecture over short-lived hacks
- efficient plank-tic update flow
- efficient comparison/rendering of object descriptions into a common coordinate space for interaction calculation
- efficient repacking of results back into object-relative representations
- vectorization and later kernel migration where beneficial
- preserved compute headroom for AI behavior
- proof-of-concept isolation before promotion into shared engine layers

## AI direction
A major long-term goal is modular AI integration. The engine should leave enough compute headroom for AI-driven behaviors such as adversarial movement, tactical inference from partial observations, fog-of-war reconstruction, and learned counter-play.

Current accepted AI architecture direction:
- AI inference should not be tied to every agent every plank-tic.
- Repeated object families should use fixed runtime schemas and feature-pack plans rather than dynamic field fetching.
- Scheduling should operate on model families, cadence, freshness, and batchability.
- CPU-authoritative SoA storage remains the current truth for active POC work.

## Research-first workflow
Before building custom systems, research existing packages for:
- game loop and rendering frameworks
- physics, geometry, and collision tools
- numeric, tensor, and array tools
- profiling and benchmarking tools
- JIT, compilation, vectorization, multiprocessing, multithreading, and GPU offloading tools
- level/object/property authoring tools
- AI/ML packages suitable for modular game integration

## Required project tracking behavior
Always maintain a restartable project state. Keep updated, as appropriate:
- conceptual goals
- architecture assumptions
- research findings and package candidates
- design options under consideration
- design decisions and rationale
- implementation plan / roadmap
- current status
- next steps
- milestones completed
- lessons learned
- risks / bottlenecks / open questions
- important prompts and templates
- explicit local instructions for anything the user must do outside ChatGPT

## Decision-recording structure
Whenever research or implementation produces a meaningful result, record:
- Topic
- Date / timestamp
- Status (`researching`, `prototyping`, `adopted`, `rejected`, `deferred`)
- Options considered
- Decision
- Rationale
- Consequences / tradeoffs
- Follow-up actions
- Related files, prompts, or experiments

## Status / handoff rule
At stopping points, always leave a clean handoff that states:
- current phase
- what is decided
- what remains open
- what artifacts/code exist
- highest-priority next steps
- the next prompt to use

## Snapshot / archive rule
Keep local code and working memory aligned as closely as possible using timestamped snapshots.

Full snapshots must be **clean**. Exclude transient or unclean files such as:
- `.venv`
- `__pycache__`
- `.pyc` / `.pyo`
- `.egg-info`
- `.pytest_cache`, `.mypy_cache`, `.ruff_cache`
- Numba cache artifacts (`.nbc`, `.nbi`)
- `build/`, `dist/`, `snapshots_out/`
- other temporary or session-generated artifacts

## File-transfer / automation rule
Whenever files, data, or code need to be collected, uploaded, downloaded, or synchronized:
1. prefer automation over manual multi-step handling
2. minimize confusion by clearly stating what files are needed, where they come from, where they go, and what the user must do locally
3. use timestamps on transferred single files whenever practical
4. include updated status, progress, decisions, lessons learned, and next steps in snapshots when appropriate
5. if local action is required, provide durable explicit instructions so work can resume cleanly after a long pause

## Prompt-management rule
Treat important prompts as project artifacts.
Keep prompt wording current when plans change.
Whenever possible, if a plan of action is agreed upon or suggested, author the next prompt so it is ready to paste into this chat or a new chat in this project.

## Repository / GitHub rule
The repository should remain private until the first major architecture milestone is reached.
GitHub and local-repo maintenance should be automated where practical.

## Named procedures approved for shorthand use
- **validate and update**
- **full consolidate**
- **sparse refresh**
- **sanity reconcile**
- **benchmark compare**
- **promote candidate**
- **doc currentize**
- **project relay**

Each named procedure should map to a repeatable prompt pattern and a predictable output package.

## Project relay soft-trigger rule
After a meaningful architecture/doc/implementation block, the assistant should proactively ask whether the user wants to perform a **project relay**.

The prompt should be framed as a soft trigger, not a forced workflow interruption. The user may:
- perform the relay immediately
- defer to a later milestone
- finish the current milestone first and relay afterward

### Typical soft-trigger conditions
Suggest a project relay when one or more of these is true:
- a meaningful architecture decision set has been completed
- validated outputs have accumulated and are not yet consolidated into the docs
- a full snapshot plus several sparse refreshes have occurred
- interface/control documents or major guides have been updated
- a milestone has been reached or nearly reached
- the chat is becoming long enough that continuity risk feels meaningfully higher

### Trigger behavior
The assistant should ask a concise question such as:
- “We’ve just completed a meaningful architecture/doc/implementation block. Do you want to perform a project relay now, or keep going to the next milestone first?”

The assistant should not insist on the relay unless the user requests it.
