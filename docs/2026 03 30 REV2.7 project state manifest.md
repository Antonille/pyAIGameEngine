# 2026 03 30 REV2.7 project state manifest

## Workspace version
0.0.18

## POC package version
0.1.10

## Current truth summary
- POC1 fallback path remains authoritative
- PyBullet remains optional/deferred
- research sweep remains a documented procedure for checking architecture direction before widening implementation
- packet boundary semantics now include versioned generations, baseline references, supersede references, explicit cache outcomes, and support for replace / delta / invalidate / no_change / delete modes
- explicit `no_change` behavior is now exercised by a dedicated hold/steady family and is visible in demo and benchmark output
- the pre-`no_change` packet-cache benchmark compare doc remains valid as a historical subset
- the current validated benchmark subset for the no-change-enabled workload shows:
  - `500 / 1024`: warmed **Numba** wins
  - `2000 / 1024`: warmed **Numba** wins
- packet-layer metrics for the compared runs match across backends, so the current winner claim is being driven by stage timings rather than packet/cache divergence

## Current phase
- documentation/control-boundary formalization complete for model/AI/UI roles
- multi-family runtime-schema / feature-pack / transfer-planner boundary implemented and validated
- versioned action-packet bridge, packet cache, and packet-state modes implemented and validated
- explicit `no_change` path and tighter baseline-aware semantics implemented and benchmark-compared
- next major work block: packet ownership / baseline refinement or project relay

## Highest-priority next step
Refine packet ownership and baseline-aware semantics now that the explicit no-change-enabled workload has a recorded benchmark subset and a cleaner current backend claim.
