# 2026 03 29 REV2.0 Optimized Prompts and Templates

## Durable prompt structure
The most effective prompts in this project usually separate:
1. current-truth anchor
2. pass type
3. constraints / deferrals
4. required outputs
5. exact validation expectations
6. next-step handoff

## Research pass template
Use the current pyAIGameEngine project state as truth.

Pass type:
Research pass.

Task:
Investigate the best package/tool/design options for the specified subsystem before new implementation work.

Requirements:
1. Preserve current validated project truth.
2. Compare realistic candidate options.
3. State what is recommended now, what is plausible later, and what should be deferred.
4. Record tradeoffs, risks, and follow-up actions.
5. Update the relevant docs and provide the next recommended prompt.

## Architecture/spec pass template
Use the current pyAIGameEngine project state as truth.

Pass type:
Architecture/spec pass.

Task:
Refine or extend the architecture/specification for the requested subsystem.

Requirements:
1. Align with current validated runtime truth and current workflow rules.
2. Do not overcommit to a broader refactor unless justified by validated boundary work.
3. Distinguish simulation-authoritative structures from interface-facing structures where relevant.
4. Identify recommended-now vs deferred items.
5. Update the relevant docs and provide the next recommended prompt.

## Implementation pass template
Use the current pyAIGameEngine project state as truth.

Pass type:
Implementation pass.

Task:
Implement the requested prototype or refinement.

Requirements:
1. Preserve fallback-first architecture and keep PyBullet optional/deferred unless validated work changes that truth.
2. Keep current benchmark/runtime truth intact unless explicitly superseded by new validated results.
3. Update code, docs, workflow artifacts, and prompts as needed.
4. Deliver exact local validation commands and expected high-level outcomes.
5. Provide the next recommended prompt.

## Documentation/interface pass template
Use the current pyAIGameEngine project state as truth.

Pass type:
Documentation/interface pass.

Task:
Refine the relevant control docs, developer guides, prompt artifacts, and workflow docs.

Requirements:
1. Keep the docs aligned with current validated truth.
2. Prefer clear interface/control boundaries before larger refactors.
3. Preserve restartability and handoff clarity.
4. Update current-pointer docs if needed.
5. Deliver updated docs and the next recommended prompt.

## Project relay template
Use the current pyAIGameEngine project state as truth.

Pass type:
Relay/handoff pass.

Task:
Perform the named procedure **project relay**.

Requirements:
1. Reconcile current chat truth against the latest snapshot.
2. Refresh current-truth docs and current-pointer docs.
3. Package the project cleanly for transfer to a new chat.
4. Deliver the snapshot, bootstrap prompt, local instructions, and next recommended prompt.

## Chat retrospective template
Use the current pyAIGameEngine project state as truth.

Pass type:
Retrospective/process-improvement pass.

Task:
Perform the named procedure **chat retrospective**.

Requirements:
1. Extract additional workflow, prompting, validation, relay, benchmark-interpretation, and interface-timing lessons from the completed chat block.
2. Update:
   - Project Resource Guide
   - Lessons Learned
   - optimized prompts/templates
   - workflow docs
3. Add or refine a short workflow-heuristics section where helpful.
4. Identify which lessons should also be promoted into the Prompt Optimization and Workflow Engineering knowledge base.
5. Deliver:
   - updated docs
   - concise summary of new lessons
   - recommended next prompt

## Current recommended implementation prompt
Use the current pyAIGameEngine project state as truth.

Pass type:
Implementation pass.

Task:
Implement the first runtime schema + feature-block/feature-pack + transfer-planning prototype pass.

Requirements:
1. Add one repeated object-family runtime schema that uses:
   - fixed hot-path runtime fields
   - SoA-friendly layout
   - optional sparse property/payload handles
2. Add feature-block schemas for that family.
3. Add at least one feature-pack plan for one AI model family.
4. Add a transfer-planning prototype that maps:
   - scheduled AI families
   - eligible entity indices
   - required feature blocks
   - staged copy groups / transfer groups
5. Keep the design aligned with the current AI scheduling direction:
   - cadence
   - freshness
   - fallback policy
   - batchability
6. Keep benchmark comparison tooling current.
7. Preserve fallback-first architecture and keep PyBullet optional/deferred.
8. Update docs, workflow, manifest, modification history, and next-step prompts.
9. Deliver:
   - sparse snapshot ZIP
   - updated code
   - updated docs
   - exact local validation commands
   - next recommended prompt
