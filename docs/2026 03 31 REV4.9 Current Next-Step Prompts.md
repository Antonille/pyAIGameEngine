# 2026 03 31 REV4.9 Current Next-Step Prompts

## Primary next prompt
Open `../OPEN_THIS_REPO_FIRST.md` first and use GitHub `main` as repo-truth for pyAIGameEngine when GitHub connector access is available.

Preferred mode:
**repo-write relay** (preferred as of 2026-03-31 when GitHub connector access is available).

Pass type:
Validation / implementation-review pass.

Task:
Validate the first bounded configurable-interface groundwork slice on the actual Windows `.venv`, confirm fail-fast behavior, confirm testing/reporting metadata capture, and assess whether the compiled startup plan stays performance-neutral.

Required work:
1. run the manifest validation script and regenerate the human-readable interface doc
2. run fallback validation and integrated core tests
3. confirm the integrated test archive/report now records configurable-interface metadata
4. confirm the current report reflects interface validation status
5. identify any stale doc/report surfaces still missing the configurable-interface decision
6. preserve fallback-first CPU-authoritative truth and PyBullet optional / deferred truth
7. leave the next recommended prompt based on validation results
