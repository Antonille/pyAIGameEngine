# 2026 03 30 REV4.2 AI-RL Integration Strategy

## Purpose
This document defines how AI and reinforcement learning should be integrated into pyAIGameEngine without destabilizing the engine and simulation foundations.

## Core strategy
AI/RL remains **downstream of engine credibility**.
The engine should first provide:
- stable runtime schemas
- stable packet/action interfaces
- repeatable benchmark/verification behavior
- controlled scenario definitions
- a thin adapter boundary for future RL-facing use
- long-lifecycle archival test data and regenerable reporting

## Current accepted integration truth
- The project already has the beginnings of the right AI boundary in code.
- The main near-term risk is **boundary drift**, not lack of primitives.
- The current minimal Gym-compatible path is temporary scaffolding and should not harden into the de facto public AI API.
- The future-correct observation path is **runtime-schema-driven feature blocks extracted from SoA-authoritative state**.
- The intended authoritative action path is:
  1. action packets
  2. action-bridge application
  3. authoritative write into `state.action`
  4. application/physics consumption
- Control cadence is currently a derived layer over the authoritative plank-tic loop.
- Deterministic or tightly repeatable fallback-first CPU behavior should become an explicit near-term target for:
  - fixed scenario
  - fixed seed
  - fixed action stream
- Headless parallel rollout should stay future-compatible, but should not distort the current milestone before reset/determinism/reporting discipline is tighter.

## Integration order
### Stage 1 — scripted / heuristic baselines
Start with:
- simple scripted policies
- deterministic or low-variance scenario checks
- packet/action behavior that is easy to inspect manually
- repeatable archival recording of run metadata and outcomes

### Stage 2 — AI family experimentation
Use the current family/cadence/freshness/batchability model to exercise:
- different feature packs
- different packet contracts
- stable train-time input/output assumptions

### Stage 3 — environment adapter readiness
Introduce a thin project-local adapter that:
- wraps the current engine-side loop
- uses contract-facing observations only
- preserves the accepted action boundary
- supports explicit `train`, `eval`, and `benchmark` modes
- supports named baseline policies
- records seed and mode as first-class metadata

### Stage 4 — RL experimentation
After adapter readiness:
- keep training and evaluation environments logically separated
- start with simple tasks and narrow reward structures
- progress from simple to complex environments deliberately
- preserve benchmarkable baseline policies for comparison
- keep experiment/run artifacts reproducible and versioned

## Immediate control documents
This strategy is now paired with:
- `docs/CURRENT_AI_INTERFACE_CONTROL.md`
- `docs/CURRENT_MODEL_INTERFACE_CONTROL.md`
- `docs/CURRENT_RESET_DETERMINISM_REPLAY_SPEC.md`
- `docs/CURRENT_TIMING_AND_PROFILING_CONTRACT.md`
- `docs/CURRENT_ENV_ADAPTER_REFIT_PLAN.md`

## Current stance
- do not freeze a public RL API around the current Gym path
- do not prematurely commit to many-world execution
- do not leak model-specific logic into the hot simulation core
