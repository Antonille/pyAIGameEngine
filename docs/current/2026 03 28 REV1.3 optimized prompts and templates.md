# 2026 03 28 REV1.3 Optimized Prompts and Templates

## Chat bootstrap template
Use the current pyAIGameEngine project state as truth.
Read the current Project Resource Guide, current workflow, current project state manifest, and current next-step prompts before proposing changes.
Preserve the single Python 3.12 `.venv` baseline and fallback-first POC1 truth unless explicitly told to revise them.
Archive superseded docs rather than leaving ambiguous duplicates in active paths.
When delivering updates, append the file modification history and leave a clean handoff.

## Named-procedure prompt stubs
### validate and update
Use the uploaded snapshot as the new candidate truth. Perform a consistency and sanity check against the expected project state, report mismatches, reconcile accepted differences, update docs/versioning/tracking, append file modification history, and return a sparse or full updated snapshot.

### full consolidate
Use the current project state as truth. Refresh active docs, archive superseded docs, align scripts and manifests, remove transient artifacts, append file modification history, and deliver a full clean snapshot.

### sparse refresh
Use the current project state as truth. Apply the requested changes, update only affected docs/scripts/code, append file modification history, and deliver a sparse snapshot with exact copy-in instructions.

### benchmark compare
Use the current project state as truth. Run or prepare a NumPy vs Numba comparison pass, summarize timing, identify bottlenecks, and update the relevant docs/manifests.
