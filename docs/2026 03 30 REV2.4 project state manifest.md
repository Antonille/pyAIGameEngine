# 2026 03 30 REV2.4 project state manifest

## Workspace version
0.0.17

## POC package version
0.1.9

## Current truth summary
- POC1 fallback path remains authoritative
- PyBullet remains optional/deferred
- interface-definition docs remain the contract-facing guidance for model, AI, and future UI roles
- project relay and chat retrospective remain named, documented handoff/refinement procedures with soft-trigger policy
- research sweep remains a documented procedure for checking architecture direction before widening implementation
- current validated truth includes successful fallback validation, successful rigid-body/field prototype run, successful multi-family runtime-schema / feature-pack / transfer-planner validation, and successful packet-cache / packet-state-mode validation at the action boundary
- packet boundary semantics now include versioned generations, baseline references, explicit cache outcomes, and support for replace / delta / invalidate / no_change / delete modes
- current benchmark truth for the packet-cache workload favors warmed Numba in the validated 500-step and 2000-step / 1024-body comparisons, while the project still records benchmark claims with workload context rather than permanent single-winner language

## Current phase
- documentation/control-boundary formalization complete for model/AI/UI roles
- multi-family runtime-schema / feature-pack / transfer-planner boundary implemented and validated
- versioned action-packet bridge, packet cache, and packet-state modes implemented and validated
- benchmark tooling updated to report packet-layer cache metrics
- next major work block: benchmark compare pass for the packet-cache workload matrix and documentation refresh of the current winner claims

## Highest-priority next step
Run a benchmark compare pass for the packet-cache workload and currentize the benchmark truth in the docs before widening the implementation branch again.
