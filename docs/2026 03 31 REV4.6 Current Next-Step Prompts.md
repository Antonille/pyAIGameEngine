# 2026 03 31 REV4.6 Current Next-Step Prompts

## Primary next prompt
Use GitHub main as repo-truth for pyAIGameEngine.

Pass type:
Implementation/maintenance pass.

Task:
Perform the bounded line-ending and canonical-env-var hardening pass.
Do not do new engine/AI feature work in this pass.

Pre-pass retrieval required:
1. Read `docs/CURRENT_LESSONS_LEARNED.md`
2. Read `docs/CURRENT_LESSON_ACTION_REGISTER.md`
3. State which open actions are being addressed

Required work:
A. Add a root `.gitattributes` file that:
   - keeps PowerShell / batch files as `CRLF`
   - keeps Python, Markdown, JSON, YAML, TOML, HTML, SVG, and other cross-platform text as `LF`
   - marks binary formats as binary
B. Run and document the one-time renormalization workflow.
C. Patch Windows scripts to use only:
   - `path_pyAIGameEngine`
   - `path_pyEngine_scripts`
   - `path_revs_pyAIGameEngine`
   and remove all references to retired revisions aliases.
D. Update the Windows scripts README to match the actual rewritten script behavior.
E. Leave exact validation commands and a relay-quality handoff.
