# 2026 03 29 REV1.7 Current Workflow

## Current phase
Early architecture definition + validated fallback POC1 + initial interface-definition pass.

## Decision for this pass
Now is the right time to formalize:
- interface control documents
- developer guides
- lightweight façade planning

Now is **not** yet the right time for:
- a major independent package split
- freezing final external APIs across the whole repo
- a heavy UI subsystem refactor

## Why
Latest local validation and prototype output show enough architectural stability to define boundaries confidently, including:
- fallback validation
- NumPy/Numba comparison tooling
- scheduler prototype
- rigid-body / field prototype outputs

## Near-term sequence
1. Interface control documents
2. Developer guides
3. Lightweight façade modules
4. Runtime schema + feature-pack + transfer planner implementation
5. First real refactor after those pieces are validated together

## Local review steps
1. Read:
   - `docs/CURRENT_PROJECT_RESOURCE_GUIDE.md`
   - `docs/CURRENT_MODEL_INTERFACE_CONTROL.md`
   - `docs/CURRENT_AI_INTERFACE_CONTROL.md`
   - `docs/CURRENT_UI_INTERFACE_PLAN.md`
   - `docs/CURRENT_MODEL_DEVELOPER_GUIDE.md`
   - `docs/CURRENT_AI_DEVELOPER_GUIDE.md`
2. Import the lightweight façade stubs:
```powershell
.\.venv\Scripts\python.exe -c "from poc1_engine.interfaces import ModelArtifact, ScenarioBundle, FeatureBlockContract, AIActionPacket, RenderSnapshotView; print('interface stubs ok')"
```
3. Re-run core validation if desired:
```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\windows\run_poc1_validation.ps1 -BackendMode numpy
powershell -ExecutionPolicy Bypass -File .\scripts\windows\run_poc1_validation.ps1 -BackendMode numba
```
