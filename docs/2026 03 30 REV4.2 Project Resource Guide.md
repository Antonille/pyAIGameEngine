# 2026 03 30 REV4.2 Project Resource Guide

## Purpose
This is the current high-level resource document for pyAIGameEngine. It is the best single starting point for current architecture, workflow, status, and project operating rules.

## Current project state
- Workspace version: **0.0.22**
- Active proof of concept: **POC1_SoaFirst**
- POC package version: **0.1.13**
- Python baseline: **single local Python 3.12 `.venv`**
- Active validated runtime path: **fallback-first CPU path**
- Native physics status: **PyBullet optional / deferred**
- GitHub repository: **`Antonille/pyAIGameEngine`**
- GitHub remote: `https://github.com/Antonille/pyAIGameEngine.git`
- Current repository status: **baseline pushed; current accepted testing truth was uploaded for this pass and should now be reconciled back into GitHub**

## Current phase
**GitHub-first workflow active + first working testing/reporting slice accepted + next likely pass is documentation/interface tightening of the AI/RL adapter boundary or follow-on implementation using accepted test truth.**

## Current validated truth
- fallback validation passes
- rigid-body / gravity / field prototype validated successfully
- Gate D scheduler / packet acceptance evidence is cleared
- fallback-first execution remains the authoritative path for active POC work
- the first integrated long-lifecycle testing/reporting slice is now accepted as working
- adapter-facing rollout evidence is now part of accepted project truth
- accepted 500 / 1024 integrated benchmark evidence currently shows numpy and numba are close enough that no decisive winner should be frozen yet
- accepted 512-body adapter-facing benchmark evidence shows AI/scheduling/packet-side orchestration remains the largest stage cost in that benchmark shape

## Workflow heuristics
- Use a clearly named **pass type** whenever practical.
- Use GitHub + current-pointer docs as the shared truth anchor when the intended baseline is pushed.
- Use uploaded archival test outputs as immediate truth when they are newer than the repo.
- Preserve fallback-first execution and PyBullet deferral unless validated work changes that truth.
- Treat archival test data as append-only durable truth and the current report as regenerable.
- Avoid overclaiming performance trends until more apples-to-apples history exists.

## Named procedures currently most relevant
- `doc currentize`
- `benchmark compare`
- `project relay`
- `chat retrospective`
- `promote candidate`
