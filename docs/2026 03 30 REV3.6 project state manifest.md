# 2026 03 30 REV3.6 project state manifest

## Snapshot identity
- Snapshot type: **docs-only sparse update**
- Workspace version: **0.0.20**
- Active proof of concept: **POC1_SoaFirst**
- POC package version: **0.1.12**
- Python baseline: **single local Python 3.12 `.venv`**

## Current authoritative runtime truth
- fallback-first CPU path remains authoritative
- PyBullet remains optional / deferred
- scheduler and packet-lane behavior has explicit Gate D acceptance evidence
- for the latest validated ownership-aware `no_change` / baseline-ownership-targeted benchmark subset:
  - `500 / 1024`: warmed **Numba** wins
  - `2000 / 1024`: warmed **Numba** wins

## Current architecture/spec truth
- Phase 4 Gate A is now the next implementation gate
- the first adapter-facing boundary should be thin, non-authoritative, and contract-facing
- the first baseline policy scope should be hold / steady plus one simple reflex policy
- `train`, `eval`, and `benchmark` must be explicitly tagged modes
- first adapter-facing artifact fields should include scenario version, adapter version, seed, and baseline policy id

## Intended next action
Run the Gate A implementation pass.
