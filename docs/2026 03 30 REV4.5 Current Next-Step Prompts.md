# 2026 03 30 REV4.5 Current Next-Step Prompts

## Primary next prompt
Use the cleaned pyAIGameEngine full snapshot as local snapshot-truth and GitHub main as repo-truth.

Pass type:
Implementation/maintenance pass.

Task:
Perform the bounded snapshot-selector hardening and repo-sync pass.
Do not do new engine/AI feature work in this pass.

Pre-pass retrieval required:
1. Read `docs/CURRENT_LESSONS_LEARNED.md`
2. Read `docs/CURRENT_LESSON_ACTION_REGISTER.md`
3. State which open actions are being addressed

Required work:
A. Replace brittle latest-snapshot selection with explicit compatible scripts for:
   - `pwsh`
   - Windows PowerShell 5.1
B. Add deterministic `Kind` handling (`full|sparse|patch|any`) and utility-ZIP exclusion.
C. Update docs/workflow to reflect the new selection contract.
D. Verify GitHub entry points after the cleaned snapshot is promoted.
E. Leave exact validation commands and a relay-quality handoff.
