# 2026 03 30 REV3.5 Optimized Prompts and Templates

## Durable prompt structure
The most effective prompts in this project usually separate:
1. current-truth anchor
2. pass type
3. constraints / deferrals
4. required outputs
5. exact validation expectations
6. next-step handoff

## Gate-clearing implementation template
Use the uploaded pyAIGameEngine candidate snapshot as the current project truth.

Pass type:
Implementation pass.

Task:
Clear the named phase gate.

Requirements:
1. Name the gate explicitly.
2. Add or strengthen explicit acceptance checks tied to that gate.
3. Preserve fallback-first architecture and keep PyBullet optional/deferred unless validated work changes that truth.
4. Keep current benchmark/runtime truth intact unless explicitly superseded by a benchmark-compare pass.
5. Update:
   - Lifecycle and Phase Gates
   - Verification and Validation Matrix
   - Current Workflow
   - modification history
   - project state manifest
   - Current Next-Step Prompts
6. Deliver:
   - sparse snapshot ZIP
   - updated code and docs
   - exact local validation commands
   - concise gate-clearing conclusion
   - next recommended prompt

## Current recommended next prompt
Use the uploaded pyAIGameEngine candidate snapshot as the current project truth.

Pass type:
Architecture/spec pass.

Task:
Define Phase 4 Gate A — Environment Adapter and Baseline Policy Readiness before reopening broader AI/RL-facing code work.
