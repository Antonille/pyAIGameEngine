# 2026 03 30 REV3.8 Current Workflow

## Current phase
Post-Gate-D / Gate-A-defined state with research-aligned workflow hardening and testing/reporting design refinement.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to **current validated truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide **exact validation commands** where validation is relevant.
6. Update current-truth docs, lessons, prompts, manifest/history, and handoff artifacts.
7. Ask whether to use `project relay` or `chat retrospective` when a meaningful block has just completed.

## Research-sweep refinement now in force
When the project is broadening, a research sweep should include:
1. main architecture-alignment research
2. at least one complementary topic on workflow/tooling/testing not covered by the first sweep
3. recommended now / plausible later / deferred categorization
4. direct implications for prompts, test design, and documentation

## Testing/reporting workflow guidance now in force
The long-lifecycle testing/reporting system should be treated as cross-cutting infrastructure.
It can advance in parallel with gate-specific implementation as long as it does not destabilize current runtime truth.

Required design rules:
- archival test data is append-only
- current human-readable reports are regenerable
- apples-to-apples benchmark history must use explicit compatibility keys and stable subsets
- smoke, representative, acceptance, and regression-style runs must remain distinguishable

## Near-term sequence now recommended
1. Take the remaining documentation steps needed to align research, workflow, and prompt artifacts
2. Implement the first working slice of the long-lifecycle integrated testing and reporting system
3. Keep Gate A and later adapter-facing work aligned with that reporting/test infrastructure
4. Revisit ASV / DVC / MLflow only after the custom archival truth model and harness skeleton exist
