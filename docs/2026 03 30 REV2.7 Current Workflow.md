# 2026 03 30 REV2.7 Current Workflow

## Current phase
Early architecture definition + validated fallback POC1 + interface-definition pass complete + research-sweep validation of the current direction + packet-cache semantics and packet-state modes implemented at the action boundary + explicit `no_change` path and tighter baseline-aware packet semantics implemented + first benchmark subset for the no-change-enabled workload recorded and interpreted.

## Default work cadence
1. Choose the **pass type**.
2. Anchor the work to **current validated truth**.
3. State what should **not** be overcommitted yet.
4. Execute the code/doc/spec pass.
5. Provide **exact validation commands** where validation is relevant.
6. Update current-truth docs, lessons, prompts, and handoff artifacts.
7. Ask whether to use `project relay` or `chat retrospective` when a meaningful block has just completed.

## Research sweep guidance
Use `research sweep` when:
- the benchmark signal is mixed
- the architecture branch is about to widen
- a new tool/backend path is being considered
- the current design needs an outside-evidence check before more implementation

## Relay trigger guidance
The assistant should ask about `project relay` after a meaningful architecture/doc/implementation block, especially after:
- validated outputs and doc refreshes
- several sparse refreshes
- interface/control document changes
- milestone completion or near-completion

This should be a soft question, not a forced stop.

## Chat retrospective guidance
Use `chat retrospective` when a just-completed block generated reusable lessons about:
- prompt structure
- workflow efficiency
- validation discipline
- benchmark interpretation
- relay timing
- interface/control-boundary timing

## Benchmark compare guidance
Use `benchmark compare` when:
- the integrated winner claim may have changed
- a new family, packet contract, or cache policy has changed the workload mix
- stage-level and packet-level metrics need to be reconciled into current project truth
- the next implementation step depends on knowing whether the current backend story is steady or mixed

A benchmark compare pass should:
1. review current-pointer docs first
2. run the agreed benchmark matrix or the next meaningful subset of it
3. record both stage-level and packet-layer metrics
4. state the current winner claim **with workload context**
5. identify whether the current benchmark subset is:
   - sanity/instrumentation
   - medium representative
   - long representative
6. update docs, lessons, prompts, and next-step guidance if the benchmark interpretation changed

## Current implication of the latest benchmark compare block
- The explicit `no_change` path is now present and benchmark-visible.
- The first no-change-enabled benchmark subset no longer supports the earlier tentative “mixed” claim for the validated medium and long runs.
- For the current validated `500 / 1024` and `2000 / 1024` subset, warmed **Numba** is the integrated winner.
- Packet-layer metrics are matched across backends for the compared runs, so the winner claim is currently being driven by stage timings rather than different packet/cache behavior.
- The current best next step is an **implementation** or **architecture/spec** pass focused on whether ownership/baseline semantics need refinement now that the packet boundary is benchmark-visible and the current winner claim is cleaner.

## Near-term sequence after this benchmark compare pass
1. Decide whether delete/invalidate semantics need additional ownership refinement
2. Decide whether the packet benchmark matrix should widen before more packet-feature complexity is added
3. Delay larger refactors until the packet boundary is proven across richer semantics and a broader benchmark matrix if needed
4. Consider `project relay` after the next meaningful implementation/doc-refresh block
