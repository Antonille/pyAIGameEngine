# 2026 03 30 REV2.4 Optimized Prompts and Templates

## Durable prompt structure
The most effective prompts in this project usually separate:
1. current-truth anchor
2. pass type
3. constraints / deferrals
4. required outputs
5. exact validation expectations
6. next-step handoff

## Research pass template
Use the current pyAIGameEngine project state as truth.

Pass type:
Research pass.

Task:
Investigate the best package/tool/design options for the specified subsystem before new implementation work.

Requirements:
1. Preserve current validated project truth.
2. Compare realistic candidate options.
3. State what is recommended now, what is plausible later, and what should be deferred.
4. Record tradeoffs, risks, and follow-up actions.
5. Update the relevant docs and provide the next recommended prompt.

## Research sweep template
Use the current pyAIGameEngine project state as truth.

Pass type:
Research sweep.

Task:
Check whether the current architecture path is still well-supported by outside evidence before widening the implementation branch.

Requirements:
1. Review the current-pointer docs first.
2. Formulate search terms around:
   - project goals
   - current architecture
   - current implementation plan
   - rationale and constraints
   - lessons learned so far
3. Search for authoritative sources on similar architectures, similar goals, similar runtime strategies, and similar workflow/tooling plans.
4. Separate findings into:
   - recommended now
   - plausible later
   - deferred / not yet
5. Update working docs with:
   - research-aligned lessons
   - tool candidates and gates
   - workflow/prompt changes
   - recommended next prompt
6. Prefer specification refinement over immediate implementation when the benchmark signal is mixed.

## Architecture/spec pass template
Use the current pyAIGameEngine project state as truth.

Pass type:
Architecture/spec pass.

Task:
Refine or extend the architecture/specification for the requested subsystem.

Requirements:
1. Align with current validated runtime truth and current workflow rules.
2. Do not overcommit to a broader refactor unless justified by validated boundary work.
3. Distinguish simulation-authoritative structures from interface-facing structures where relevant.
4. Identify recommended-now vs deferred items.
5. Update the relevant docs and provide the next recommended prompt.

## Implementation pass template
Use the current pyAIGameEngine project state as truth.

Pass type:
Implementation pass.

Task:
Implement the requested prototype or refinement.

Requirements:
1. Preserve fallback-first architecture and keep PyBullet optional/deferred unless validated work changes that truth.
2. Keep current benchmark/runtime truth intact unless explicitly superseded by new validated results.
3. Update code, docs, workflow artifacts, and prompts as needed.
4. Deliver exact local validation commands and expected high-level outcomes.
5. Provide the next recommended prompt.

## Benchmark compare template
Use the current pyAIGameEngine project state as truth.

Pass type:
Benchmark compare.

Task:
Run the next meaningful benchmark subset or matrix and reconcile the current winner claim with workload context.

Requirements:
1. Review the current-pointer docs first.
2. Preserve fallback-first architecture and keep PyBullet optional/deferred.
3. Record both stage-level and packet-layer metrics.
4. State the current winner claim with workload context, not as timeless truth.
5. Update docs, lessons learned, workflow, prompts, and next-step guidance if the benchmark interpretation changed.
6. Deliver exact local commands, concise conclusions, and the next recommended prompt.

## Documentation/interface pass template
Use the current pyAIGameEngine project state as truth.

Pass type:
Documentation/interface pass.

Task:
Refine the relevant control docs, developer guides, prompt artifacts, and workflow docs.

Requirements:
1. Keep the docs aligned with current validated truth.
2. Prefer clear interface/control boundaries before larger refactors.
3. Preserve restartability and handoff clarity.
4. Update current-pointer docs if needed.
5. Deliver updated docs and the next recommended prompt.

## Project relay template
Use the current pyAIGameEngine project state as truth.

Pass type:
Relay/handoff pass.

Task:
Perform the named procedure **project relay**.

Requirements:
1. Reconcile current chat truth against the latest snapshot.
2. Refresh current-truth docs and current-pointer docs.
3. Package the project cleanly for transfer to a new chat.
4. Deliver the snapshot, bootstrap prompt, local instructions, and next recommended prompt.

## Chat retrospective template
Use the current pyAIGameEngine project state as truth.

Pass type:
Retrospective/process-improvement pass.

Task:
Perform the named procedure **chat retrospective**.

Requirements:
1. Extract additional workflow, prompting, validation, relay, benchmark-interpretation, and interface-timing lessons from the completed chat block.
2. Update:
   - Project Resource Guide
   - Lessons Learned
   - optimized prompts/templates
   - workflow docs
3. Add or refine a short workflow-heuristics section where helpful.
4. Identify which lessons should also be promoted into the Prompt Optimization and Workflow Engineering knowledge base.
5. Deliver:
   - updated docs
   - concise summary of new lessons
   - recommended next prompt

## Current recommended benchmark-compare prompt
Use the uploaded pyAIGameEngine snapshot as the current project truth.

Before doing new implementation work:
1. Review the current-pointer docs first:
   - `docs/CURRENT_PROJECT_INSTRUCTIONS.md`
   - `docs/CURRENT_PROJECT_RESOURCE_GUIDE.md`
   - `docs/CURRENT_WORKFLOW.md`
   - `docs/CURRENT_PROJECT_RELAY.md`
   - `docs/CURRENT_PROJECT_RELAY_PROMPT.md`
   - `docs/CURRENT_CHAT_RETROSPECTIVE.md`
   - `docs/CURRENT_LESSONS_LEARNED.md`
   - `docs/CURRENT_OPTIMIZED_PROMPTS_AND_TEMPLATES.md`
   - `docs/CURRENT_RESEARCH_SWEEP.md`
   - `docs/CURRENT_NEXT_STEP_PROMPTS.md`
2. Treat those docs as the authoritative summary of project status, workflow, lessons learned, prompt artifacts, and current best practices.
3. Preserve fallback-first architecture and keep PyBullet optional/deferred unless validated work changes that truth.
4. Preserve the latest validated benchmark/runtime truth already recorded in the docs.
5. Do not re-derive project truth unnecessarily if it is already captured in the current docs.

Pass type:
Benchmark compare.

Task:
Run and interpret the first packet-cache benchmark matrix for the current workload.

Requirements:
1. Compare at least the current backend pair across multiple run lengths.
2. Record stage-level and packet-layer metrics, including cache hits/misses/invalidations and reuse ratio.
3. State the current NumPy vs Numba winner claim with workload context.
4. Update docs, lessons learned, workflow, and next-step prompts.
5. Deliver:
   - sparse snapshot ZIP
   - updated docs
   - exact local validation commands
   - concise benchmark conclusions
   - next recommended prompt
