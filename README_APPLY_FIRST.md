Apply this patch into the pyAIGameEngine repo root.

Primary purpose:
- add the first AI/RL boundary control-document set
- update current-pointer docs to point to the new REV4.2 docs
- refresh stale GitHub-transition transition notes now that the repo baseline is active
